generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}


model Organization {
  id          String   @id @default(cuid())
  name        String
  shortName   String?  @map("short_name")
  code        String   @unique
  parentId    String?  @map("parent_id")
  parent      Organization? @relation("OrgHierarchy", fields: [parentId], references: [id])
  children    Organization[] @relation("OrgHierarchy")
  isActive    Boolean  @default(true) @map("is_active")
  description String?
  attachments String     @default("[]")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  taxSubjects  TaxSubject[]
  users        User[]
  customers    Customer[]
  suppliers    Supplier[]
  businessOrders BusinessOrder[]
  costCenters      CostCenter[]
  advancePayments  AdvancePayment[]

  @@map("organizations")
}

model User {
  id             String   @id @default(cuid())
  name           String
  email          String   @unique
  emailVerified  DateTime? @map("email_verified")
  passwordHash   String?  @map("password_hash")
  image          String?
  role           String   @default("user")
  organizationId String   @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id])
  isActive       Boolean  @default(true) @map("is_active")
  createdAt      DateTime @default(now()) @map("created_at")
  updatedAt      DateTime @updatedAt @map("updated_at")

  outputInvoiceApplications OutputInvoiceApplication[] @relation("CreatedByUser")
  advancePayments          AdvancePayment[] @relation("CreatedByAdvancePayments")
  operationLogs            OperationLog[]

  @@map("users")
}

model TaxSubject {
  id             String       @id @default(cuid())
  name           String
  taxNo          String       @map("tax_no")
  address        String?
  phone          String?
  bankName       String?      @map("bank_name")
  bankAccount    String?      @map("bank_account")
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id])
  isDefault      Boolean      @default(false) @map("is_default")
  isActive       Boolean      @default(true) @map("is_active")
  attachments    String         @default("[]")
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  outputInvoiceApplications OutputInvoiceApplication[]
  outputInvoices            OutputInvoice[]
  preInvoices               PreInvoice[]
  bankAccounts              TaxSubjectBankAccount[]

  @@map("tax_subjects")
}

model Customer {
  id             String       @id @default(cuid())
  name           String
  shortName      String?      @map("short_name")
  taxNo          String?      @map("tax_no")
  address        String?
  phone          String?
  bankName       String?      @map("bank_name")
  bankAccount    String?      @map("bank_account")
  contactPerson  String?      @map("contact_person")
  contactPhone   String?      @map("contact_phone")
  contactEmail   String?      @map("contact_email")
  invoiceStrategy String @default("NO_INVOICE") @map("invoice_strategy")
  isBlacklisted  Boolean      @default(false) @map("is_blacklisted")
  isActive       Boolean      @default(true) @map("is_active")
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id])
  remark         String?
  attachments    String         @default("[]")
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  businessOrders           BusinessOrder[]
  revenueOrders            RevenueOrder[]
  advancePayments          AdvancePayment[]
  outputInvoiceApplications OutputInvoiceApplication[]
  settlements              Settlement[]
  inputInvoices            InputInvoice[]
  preInvoices              PreInvoice[]

  @@map("customers")
}

model Supplier {
  id             String       @id @default(cuid())
  name           String
  shortName      String?      @map("short_name")
  taxNo          String?      @map("tax_no")
  address        String?
  phone          String?
  bankName       String?      @map("bank_name")
  bankAccount    String?      @map("bank_account")
  contactPerson  String?      @map("contact_person")
  contactPhone   String?      @map("contact_phone")
  contactEmail   String?      @map("contact_email")
  supplierType   String?      @map("supplier_type")
  isBlacklisted  Boolean      @default(false) @map("is_blacklisted")
  isActive       Boolean      @default(true) @map("is_active")
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id])
  remark         String?
  attachments    String         @default("[]")
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  inputInvoices    InputInvoice[]
  costEntries      CostEntry[]
  advancePayments  AdvancePayment[]

  @@map("suppliers")
}

model InvoiceItem {
  id          String   @id @default(cuid())
  name        String
  category    String?
  taxCode     String?  @map("tax_code")
  taxCodeId   String?  @map("tax_code_id")
  taxCodeRel  TaxCode? @relation(fields: [taxCodeId], references: [id])
  unit        String?
  spec        String?
  isActive    Boolean  @default(true) @map("is_active")
  remark      String?
  attachments String     @default("[]")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  outputInvoiceApplicationItems OutputInvoiceApplicationItem[]
  outputInvoiceItems            OutputInvoiceItem[]
  inputInvoiceItems             InputInvoiceItem[]
  preInvoiceItems               PreInvoiceItem[]

  @@map("invoice_items")
}

model TaxCode {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  taxRate     Decimal  @default(0) @map("tax_rate")
  category    String?
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  invoiceItems InvoiceItem[]
  preInvoiceItems PreInvoiceItem[]

  @@map("tax_codes")
}

model BusinessOrder {
  id                    String      @id @default(cuid())
  orderNo               String      @unique @map("order_no")
  orderType             String      @map("order_type")
  title                 String
  customerId            String      @map("customer_id")
  customer              Customer    @relation(fields: [customerId], references: [id])
  organizationId        String      @map("organization_id")
  organization          Organization @relation(fields: [organizationId], references: [id])
  status                String @default("DRAFT")
  totalAdvanceAmount    Decimal     @default(0) @map("total_advance_amount")
  collectedAdvanceAmount Decimal    @default(0) @map("collected_advance_amount")
  uncollectedAdvanceAmount Decimal  @default(0) @map("uncollected_advance_amount")
  totalRevenueAmount    Decimal     @default(0) @map("total_revenue_amount")
  invoicedAmount        Decimal     @default(0) @map("invoiced_amount")
  availableAmount       Decimal     @default(0) @map("available_amount")
  currency              String    @default("CNY")
  startDate             DateTime?   @map("start_date")
  endDate               DateTime?   @map("end_date")
  remark                String?
  attachments           String        @default("[]")
  createdAt             DateTime    @default(now()) @map("created_at")
  updatedAt             DateTime    @updatedAt @map("updated_at")

  advancePayments  AdvancePayment[]
  revenueOrders    RevenueOrder[]
  inputInvoices    InputInvoice[]
  feeItems         FeeItem[]
  costEntries      CostEntry[]

  @@map("business_orders")
}

model RevenueOrder {
  id               String        @id @default(cuid())
  orderNo          String        @unique @map("order_no")
  title            String
  businessOrderId  String        @map("business_order_id")
  businessOrder    BusinessOrder @relation(fields: [businessOrderId], references: [id])
  customerId       String        @map("customer_id")
  customer         Customer      @relation(fields: [customerId], references: [id])
  totalRevenueAmount Decimal      @default(0) @map("total_revenue_amount")
  invoicedAmount   Decimal       @default(0) @map("invoiced_amount")
  availableAmount  Decimal       @default(0) @map("available_amount")
  invoiceStatus    String        @default("NOT_INVOICED") @map("invoice_status")
  currency         String      @default("CNY")
  startDate        DateTime?     @map("start_date")
  endDate          DateTime?     @map("end_date")
  isActive         Boolean       @default(true) @map("is_active")
  remark           String?
  attachments      String          @default("[]")
  createdAt        DateTime      @default(now()) @map("created_at")
  updatedAt        DateTime      @updatedAt @map("updated_at")

  outputInvoiceApplications OutputInvoiceApplication[]
  settlements               Settlement[]

  @@map("revenue_orders")
}

model CostCenter {
  id             String       @id @default(cuid())
  code           String       @unique
  name           String
  parentId       String?      @map("parent_id")
  parent         CostCenter?  @relation("CostCenterHierarchy", fields: [parentId], references: [id])
  children       CostCenter[] @relation("CostCenterHierarchy")
  organizationId String       @map("organization_id")
  organization   Organization @relation(fields: [organizationId], references: [id])
  manager        String?
  isActive       Boolean      @default(true) @map("is_active")
  description    String?
  createdAt      DateTime     @default(now()) @map("created_at")
  updatedAt      DateTime     @updatedAt @map("updated_at")

  inputInvoices InputInvoice[]
  costEntries   CostEntry[]

  @@map("cost_centers")
}

model FeeItem {
  id              String        @id @default(cuid())
  feeNo           String        @unique @map("fee_no")
  businessOrderId String        @map("business_order_id")
  businessOrder   BusinessOrder @relation(fields: [businessOrderId], references: [id])
  feeType         String       @map("fee_type")
  description     String?
  quantity        Decimal       @default(1)
  unitPrice       Decimal       @default(0) @map("unit_price")
  amount          Decimal       @default(0)
  currency        String      @default("CNY")
  exchangeRate    Decimal?      @default(1) @map("exchange_rate")
  baseAmount      Decimal       @default(0) @map("base_amount")
  occurredDate    DateTime      @map("occurred_date")
  isInvoiced      Boolean       @default(false) @map("is_invoiced")
  remark          String?
  attachments     String          @default("[]")
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")

  @@map("fee_items")
}

model AdvancePayment {
  id                         String                   @id @default(cuid())
  advanceNo                  String                   @unique @map("advance_no")
  organizationId             String                   @map("organization_id")
  organization               Organization?            @relation(fields: [organizationId], references: [id])
  zone                       String?                  @map("zone")
  supplierId                 String?                  @map("supplier_id")
  supplier                   Supplier?                @relation(fields: [supplierId], references: [id])
  bankAccountNo              String?                  @map("bank_account_no")
  feeType                    String                   @map("fee_type")
  occurredDate               DateTime                 @map("occurred_date")
  currency                   String                   @default("CNY")
  originalAmount             Decimal                  @default(0) @map("original_amount")
  businessOrderNo            String?                  @map("business_order_no")
  mawbNo                     String?                  @map("mawb_no")
  remark                     String?
  status                     String                   @default("DRAFT")
  collectionStatus           String                   @default("PENDING") @map("collection_status")
  writeOffStatus             String                   @default("PENDING") @map("write_off_status")
  attachments                String                   @default("[]")
  createdBy                  String?                  @map("created_by")
  creator                    User?                    @relation("CreatedByAdvancePayments", fields: [createdBy], references: [id])
  customerId                 String?                  @map("customer_id")
  customer                   Customer?                @relation(fields: [customerId], references: [id])
  businessOrderId            String?                  @map("business_order_id")
  businessOrder              BusinessOrder?           @relation(fields: [businessOrderId], references: [id])
  settlementItems            SettlementItem[]
  createdAt                  DateTime                 @default(now()) @map("created_at")
  updatedAt                  DateTime                 @updatedAt @map("updated_at")

  @@map("advance_payments")
}

model Settlement {
  id             String           @id @default(cuid())
  settlementNo   String           @unique @map("settlement_no")
  settlementType String   @map("settlement_type")
  customerId     String?          @map("customer_id")
  customer       Customer?        @relation(fields: [customerId], references: [id])
  supplierId     String?          @map("supplier_id")
  revenueOrderId String?          @map("revenue_order_id")
  revenueOrder   RevenueOrder?    @relation(fields: [revenueOrderId], references: [id])
  businessOrderId String?         @map("business_order_id")
  title          String
  periodStart    DateTime?        @map("period_start")
  periodEnd      DateTime?        @map("period_end")
  totalAmount    Decimal          @default(0) @map("total_amount")
  invoicedAmount Decimal          @default(0) @map("invoiced_amount")
  receivedAmount Decimal          @default(0) @map("received_amount")
  currency       String         @default("CNY")
  status         String @default("DRAFT")
  isAdvanceIncluded Boolean       @default(false) @map("is_advance_included")
  remark         String?
  attachments    String             @default("[]")
  createdBy      String?          @map("created_by")
  createdAt      DateTime         @default(now()) @map("created_at")
  updatedAt      DateTime         @updatedAt @map("updated_at")

  items                      SettlementItem[]
  outputInvoiceApplications  OutputInvoiceApplication[]

  @@map("settlements")
}

model SettlementItem {
  id               String          @id @default(cuid())
  settlementId     String          @map("settlement_id")
  settlement       Settlement      @relation(fields: [settlementId], references: [id])
  feeType          String         @map("fee_type")
  description      String?
  amount           Decimal         @default(0)
  taxRate          Decimal?        @map("tax_rate")
  taxAmount        Decimal         @default(0) @map("tax_amount")
  totalAmount      Decimal         @default(0) @map("total_amount")
  isInvoiced       Boolean         @default(false) @map("is_invoiced")
  advancePaymentId String?         @map("advance_payment_id")
  advancePayment   AdvancePayment? @relation(fields: [advancePaymentId], references: [id])
  feeItemId        String?         @map("fee_item_id")
  remark           String?
  createdAt        DateTime        @default(now()) @map("created_at")
  updatedAt        DateTime        @updatedAt @map("updated_at")

  @@map("settlement_items")
}

model OutputInvoiceApplication {
  id                String      @id @default(cuid())
  applicationNo     String      @unique @map("application_no")
  sourceType        String  @map("source_type")
  sourceId          String?     @map("source_id")
  revenueOrderId    String?     @map("revenue_order_id")
  revenueOrder      RevenueOrder? @relation(fields: [revenueOrderId], references: [id])
  settlementId      String?     @map("settlement_id")
  settlement        Settlement? @relation(fields: [settlementId], references: [id])
  taxSubjectId      String      @map("tax_subject_id")
  taxSubject        TaxSubject  @relation(fields: [taxSubjectId], references: [id])
  sellerTaxNo       String?     @map("seller_tax_no")
  customerId        String      @map("customer_id")
  customer          Customer    @relation(fields: [customerId], references: [id])
  buyerName         String      @map("buyer_name")
  buyerTaxNo        String?     @map("buyer_tax_no")
  buyerAddress      String?     @map("buyer_address")
  buyerPhone        String?     @map("buyer_phone")
  buyerBankName     String?     @map("buyer_bank_name")
  buyerBankAccount  String?     @map("buyer_bank_account")
  invoiceCategory   String @map("invoice_category")
  amountWithoutTax  Decimal     @default(0) @map("amount_without_tax")
  taxAmount         Decimal     @default(0) @map("tax_amount")
  amountWithTax     Decimal     @default(0) @map("amount_with_tax")
  currency          String    @default("CNY")
  exchangeRate      Decimal?    @default(1) @map("exchange_rate")
  status            String   @default("DRAFT")
  deliveryMethod    String? @map("delivery_method")
  recipientEmail    String?     @map("recipient_email")
  remark            String?
  invoiceNo         String?     @map("invoice_no")
  taxSerialNo       String?     @map("tax_serial_no")
  attachments       String        @default("[]")
  createdBy         String?     @map("created_by")
  creator           User?       @relation("CreatedByUser", fields: [createdBy], references: [id])
  approvedBy        String?     @map("approved_by")
  cashierName       String?     @map("cashier_name")
  reviewerName      String?     @map("reviewer_name")
  drawerName        String?     @map("drawer_name")
  createdAt         DateTime    @default(now()) @map("created_at")
  updatedAt         DateTime    @updatedAt @map("updated_at")

  items              OutputInvoiceApplicationItem[]
  outputInvoice      OutputInvoice?
  preInvoices        PreInvoice[]

  @@map("output_invoice_applications")
}

model OutputInvoiceApplicationItem {
  id                        String                     @id @default(cuid())
  applicationId             String                     @map("application_id")
  application               OutputInvoiceApplication   @relation(fields: [applicationId], references: [id])
  itemName                  String                     @map("item_name")
  taxClassificationCode     String?                    @map("tax_classification_code")
  invoiceItemId             String?                    @map("invoice_item_id")
  invoiceItem               InvoiceItem?               @relation(fields: [invoiceItemId], references: [id])
  spec                      String?
  unit                      String?
  quantity                  Decimal                    @default(1)
  unitPrice                 Decimal                    @default(0) @map("unit_price")
  amount                    Decimal                    @default(0)
  taxRate                   Decimal                    @default(0) @map("tax_rate")
  taxAmount                 Decimal                    @default(0) @map("tax_amount")
  totalAmount               Decimal                    @default(0) @map("total_amount")
  feeType                   String?                   @map("fee_type")
  feeItemId                 String?                    @map("fee_item_id")
  advancePaymentId          String?                    @map("advance_payment_id")
  sortOrder                 Int                        @default(0) @map("sort_order")
  createdAt                 DateTime                   @default(now()) @map("created_at")
  updatedAt                 DateTime                   @updatedAt @map("updated_at")

  @@map("output_invoice_application_items")
}

model OutputInvoice {
  id                String                     @id @default(cuid())
  invoiceNo         String                     @unique @map("invoice_no")
  invoiceCode       String?                    @map("invoice_code")
  invoiceCategory   String            @map("invoice_category")
  direction         String           @default("OUTPUT")
  blueRedFlag       String                @default("BLUE") @map("blue_red_flag")
  applicationId     String?                    @unique @map("application_id")
  application       OutputInvoiceApplication?  @relation(fields: [applicationId], references: [id])
  taxSubjectId      String                     @map("tax_subject_id")
  taxSubject        TaxSubject                 @relation(fields: [taxSubjectId], references: [id])
  sellerName        String                     @map("seller_name")
  sellerTaxNo       String                     @map("seller_tax_no")
  sellerAddress     String?                    @map("seller_address")
  sellerPhone       String?                    @map("seller_phone")
  sellerBankName    String?                    @map("seller_bank_name")
  sellerBankAccount String?                    @map("seller_bank_account")
  buyerName         String                     @map("buyer_name")
  buyerTaxNo        String?                    @map("buyer_tax_no")
  buyerAddress      String?                    @map("buyer_address")
  buyerPhone        String?                    @map("buyer_phone")
  buyerBankName     String?                    @map("buyer_bank_name")
  buyerBankAccount  String?                    @map("buyer_bank_account")
  issueDate         DateTime?                  @map("issue_date")
  businessDate      DateTime?                  @map("business_date")
  businessDescription String?                  @map("business_description")
  amountWithoutTax  Decimal                    @default(0) @map("amount_without_tax")
  taxAmount         Decimal                    @default(0) @map("tax_amount")
  amountWithTax     Decimal                    @default(0) @map("amount_with_tax")
  currency          String                   @default("CNY")
  remark            String?
  deliveryStatus    String             @default("PENDING") @map("delivery_status")
  verifyStatus      String               @default("PENDING") @map("verify_status")
  verifiedBy        String?             @map("verified_by")
  verifyTime        DateTime?           @map("verify_time")
  redFlushStatus    String?            @map("red_flush_status")
  voidStatus        String?                @map("void_status")
  accountStatus     String              @default("UNACCOUNTED") @map("account_status")
  archiveStatus         String              @default("UNARCHIVED") @map("archive_status")
  isFromTaxAuthority    Boolean             @default(false) @map("is_from_tax_authority")
  invoicePool           String              @default("OUTPUT") @map("invoice_pool")
  extendedFields        String?             @map("extended_fields")
  headerValidationStatus String             @default("PENDING") @map("header_validation_status")
  headerValidationMessage String?            @map("header_validation_message")
  usageStatus           String              @default("UNUSED") @map("usage_status")
  pushStatus            String              @default("PENDING") @map("push_status")
  pushToCBMSStatus      String              @default("PENDING") @map("push_to_cbms_status")
  emailDeliveryStatus   String              @default("PENDING") @map("email_delivery_status")
  emailSentAt           DateTime?           @map("email_sent_at")
  entryMethod           String              @default("MANUAL") @map("entry_method")
  recordedBy            String?            @map("recorded_by")
  uploadFilename        String?            @map("upload_filename")
  attachments           String              @default("[]")
  createdAt             DateTime            @default(now()) @map("created_at")
  updatedAt             DateTime            @updatedAt @map("updated_at")

  items              OutputInvoiceItem[]
  redFlushApplications RedFlushApplication[]
  voidApplications   VoidApplication[]
  deliveryRecords    InvoiceDeliveryRecord[]
  verifyRecords      InvoiceVerifyRecord[]
  invoiceFiles       InvoiceFile[]
  preInvoice         PreInvoice?

  @@map("output_invoices")
}

model OutputInvoiceItem {
  id                    String       @id @default(cuid())
  invoiceId             String       @map("invoice_id")
  invoice               OutputInvoice @relation(fields: [invoiceId], references: [id])
  itemName              String       @map("item_name")
  taxClassificationCode String?      @map("tax_classification_code")
  invoiceItemId         String?      @map("invoice_item_id")
  invoiceItem           InvoiceItem? @relation(fields: [invoiceItemId], references: [id])
  spec                  String?
  unit                  String?
  quantity              Decimal      @default(1)
  unitPrice             Decimal      @default(0) @map("unit_price")
  amount                Decimal      @default(0)
  taxRate               Decimal      @default(0) @map("tax_rate")
  taxAmount             Decimal      @default(0) @map("tax_amount")
  totalAmount           Decimal      @default(0) @map("total_amount")
  feeType               String?     @map("fee_type")
  sortOrder             Int          @default(0) @map("sort_order")
  createdAt             DateTime     @default(now()) @map("created_at")
  updatedAt             DateTime     @updatedAt @map("updated_at")

  @@map("output_invoice_items")
}

model InputInvoice {
  id                   String            @id @default(cuid())
  invoiceNo            String            @map("invoice_no")
  invoiceCode          String?           @map("invoice_code")
  invoiceCategory      String   @map("invoice_category")
  direction            String  @default("INPUT")
  blueRedFlag          String       @default("BLUE") @map("blue_red_flag")
  originalInvoiceId    String?           @map("original_invoice_id")
  sellerName           String            @map("seller_name")
  sellerTaxNo          String            @map("seller_tax_no")
  sellerAddress        String?           @map("seller_address")
  sellerPhone          String?           @map("seller_phone")
  sellerBankName       String?           @map("seller_bank_name")
  sellerBankAccount    String?           @map("seller_bank_account")
  buyerName            String            @map("buyer_name")
  buyerTaxNo           String            @map("buyer_tax_no")
  buyerAddress         String?           @map("buyer_address")
  buyerPhone           String?           @map("buyer_phone")
  buyerBankName        String?           @map("buyer_bank_name")
  buyerBankAccount     String?           @map("buyer_bank_account")
  supplierId           String?           @map("supplier_id")
  supplier             Supplier?         @relation(fields: [supplierId], references: [id])
  customerId           String?           @map("customer_id")
  customer             Customer?         @relation(fields: [customerId], references: [id])
  businessOrderId      String?           @map("business_order_id")
  businessOrder        BusinessOrder?    @relation(fields: [businessOrderId], references: [id])
  costCenterId         String?           @map("cost_center_id")
  costCenter           CostCenter?       @relation(fields: [costCenterId], references: [id])
  issueDate            DateTime?         @map("issue_date")
  businessDate         DateTime?         @map("business_date")
  businessDescription  String?           @map("business_description")
  travelServiceProvider String?          @map("travel_service_provider")
  amountWithoutTax     Decimal           @default(0) @map("amount_without_tax")
  taxAmount            Decimal           @default(0) @map("tax_amount")
  amountWithTax        Decimal           @default(0) @map("amount_with_tax")
  taxRate              Decimal?          @map("tax_rate")
  currency             String          @default("CNY")
  status               String @default("PENDING_PARSE")
  verifyStatus         String      @default("PENDING") @map("verify_status")
  verifiedBy           String?           @map("verified_by")
  verifyTime           DateTime?         @map("verify_time")
  deductStatus         String      @default("PENDING") @map("deduct_status")
  accountStatus        String     @default("UNACCOUNTED") @map("account_status")
  archiveStatus        String     @default("UNARCHIVED") @map("archive_status")
  isFromTaxAuthority   Boolean    @default(false) @map("is_from_tax_authority")
  invoicePool          String     @default("INPUT") @map("invoice_pool")
  extendedFields       String?    @map("extended_fields")
  headerValidationStatus String   @default("PENDING") @map("header_validation_status")
  headerValidationMessage String?  @map("header_validation_message")
  usageStatus          String     @default("UNUSED") @map("usage_status")
  pushStatus           String     @default("PENDING") @map("push_status")
  entryMethod          String     @default("MANUAL") @map("entry_method")
  recordedBy           String?           @map("recorded_by")
  uploadFilename       String?           @map("upload_filename")
  isAdvanceCost        Boolean    @default(false) @map("is_advance_cost")
  costType             String?          @map("cost_type")
  costAllocationRatio  Decimal?          @default(100) @map("cost_allocation_ratio")
  deductSelectionStatus String?          @map("deduct_selection_status")
  nonDeductReason      String?           @map("non_deduct_reason")
  voucherDate          DateTime?         @map("voucher_date")
  voucherNo            String?           @map("voucher_no")
  deductibleAmount     Decimal?          @map("deductible_amount")
  remark               String?
  attachments          String              @default("[]")
  createdAt            DateTime          @default(now()) @map("created_at")
  updatedAt            DateTime          @updatedAt @map("updated_at")

  items         InputInvoiceItem[]
  verifyRecords InvoiceVerifyRecord[]
  invoiceFiles  InvoiceFile[]

  @@map("input_invoices")
}

model InputInvoiceItem {
  id                    String      @id @default(cuid())
  invoiceId             String      @map("invoice_id")
  invoice               InputInvoice @relation(fields: [invoiceId], references: [id])
  itemName              String      @map("item_name")
  taxClassificationCode String?     @map("tax_classification_code")
  invoiceItemId         String?     @map("invoice_item_id")
  invoiceItem           InvoiceItem? @relation(fields: [invoiceItemId], references: [id])
  spec                  String?
  unit                  String?
  quantity              Decimal     @default(1)
  unitPrice             Decimal     @default(0) @map("unit_price")
  amount                Decimal     @default(0)
  taxRate               Decimal     @default(0) @map("tax_rate")
  taxAmount             Decimal     @default(0) @map("tax_amount")
  totalAmount           Decimal     @default(0) @map("total_amount")
  sortOrder             Int         @default(0) @map("sort_order")
  createdAt             DateTime    @default(now()) @map("created_at")
  updatedAt             DateTime    @updatedAt @map("updated_at")

  @@map("input_invoice_items")
}

model CustomsPaymentBook {
  id                   String                 @id @default(cuid())
  bookNo               String                 @unique @map("book_no")
  bookType             String  @map("book_type")
  taxType              String          @map("tax_type")
  paymentNo            String                 @map("payment_no")
  customsDeclarationNo String                 @map("customs_declaration_no")
  importPort           String?                @map("import_port")
  declarationUnit      String?                @map("declaration_unit")
  consumerUnit         String?                @map("consumer_unit")
  taxpayerName         String                 @map("taxpayer_name")
  taxpayerTaxNo        String?                @map("taxpayer_tax_no")
  taxAmount            Decimal                @default(0) @map("tax_amount")
  paymentDate          DateTime               @map("payment_date")
  currency             String               @default("CNY")
  isAdvance            Boolean                @default(false) @map("is_advance")
  advancePaymentId     String?                @map("advance_payment_id")
  deductStatus         String            @default("PENDING") @map("deduct_status")
  accountStatus        String           @default("UNACCOUNTED") @map("account_status")
  archiveStatus        String           @default("UNARCHIVED") @map("archive_status")
  remark               String?
  attachments          String                   @default("[]")
  createdAt            DateTime               @default(now()) @map("created_at")
  updatedAt            DateTime               @updatedAt @map("updated_at")

  @@map("customs_payment_books")
}

model RedFlushApplication {
  id                  String          @id @default(cuid())
  applicationNo       String          @unique @map("application_no")
  originalInvoiceId   String          @map("original_invoice_id")
  originalInvoice     OutputInvoice   @relation(fields: [originalInvoiceId], references: [id])
  redFlushType        String    @map("red_flush_type")
  redFlushReason      String  @map("red_flush_reason")
  reasonDescription   String          @map("reason_description")
  amountWithoutTax    Decimal         @default(0) @map("amount_without_tax")
  taxAmount           Decimal         @default(0) @map("tax_amount")
  amountWithTax       Decimal         @default(0) @map("amount_with_tax")
  needsReissue        Boolean         @default(false) @map("needs_reissue")
  status              String  @default("DRAFT")
  approvedBy          String?         @map("approved_by")
  confirmationFormId  String?         @unique @map("confirmation_form_id")
  confirmationForm    RedConfirmationForm? @relation("RedFlushConfirmationForm", fields: [confirmationFormId], references: [id])
  redInvoiceNo        String?         @map("red_invoice_no")
  reissueDraftId      String?         @map("reissue_draft_id")
  verifyCheckStatus   String?         @map("verify_check_status")
  bizStatusCheck      String?         @map("biz_status_check")
  attachments         String            @default("[]")
  createdAt           DateTime        @default(now()) @map("created_at")
  updatedAt           DateTime        @updatedAt @map("updated_at")

  items RedFlushItem[]

  @@map("red_flush_applications")
}

model RedFlushItem {
  id              String              @id @default(cuid())
  applicationId   String              @map("application_id")
  application     RedFlushApplication @relation(fields: [applicationId], references: [id])
  itemName        String              @map("item_name")
  originalItemId  String?             @map("original_item_id")
  amount          Decimal             @default(0)
  taxRate         Decimal             @default(0) @map("tax_rate")
  taxAmount       Decimal             @default(0) @map("tax_amount")
  totalAmount     Decimal             @default(0) @map("total_amount")
  sortOrder       Int                 @default(0) @map("sort_order")
  createdAt       DateTime            @default(now()) @map("created_at")
  updatedAt       DateTime            @updatedAt @map("updated_at")

  @@map("red_flush_items")
}

model RedConfirmationForm {
  id                      String              @id @default(cuid())
  formNo                  String              @unique @map("form_no")
  application             RedFlushApplication? @relation("RedFlushConfirmationForm")
  status                  String      @default("PENDING_CONFIRM_FORM")
  counterpartConfirmed    Boolean             @default(false) @map("counterpart_confirmed")
  counterpartConfirmedAt  DateTime?           @map("counterpart_confirmed_at")
  expiredAt               DateTime?           @map("expired_at")
  createdAt               DateTime            @default(now()) @map("created_at")
  updatedAt               DateTime            @updatedAt @map("updated_at")

  @@map("red_confirmation_forms")
}

model VoidApplication {
  id              String       @id @default(cuid())
  applicationNo   String       @unique @map("application_no")
  invoiceId       String?      @map("invoice_id")
  invoice         OutputInvoice? @relation(fields: [invoiceId], references: [id])
  voidCategory    String @map("void_category")
  voidReason      String   @map("void_reason")
  reasonDetail    String?      @map("reason_detail")
  status          String   @default("DRAFT")
  approvedBy      String?      @map("approved_by")
  executedAt      DateTime?    @map("executed_at")
  attachments     String         @default("[]")
  createdAt       DateTime     @default(now()) @map("created_at")
  updatedAt       DateTime     @updatedAt @map("updated_at")

  @@map("void_applications")
}

model InvoiceFile {
  id          String   @id @default(cuid())
  fileName    String   @map("file_name")
  filePath    String   @map("file_path")
  fileType    String   @map("file_type")
  fileSize    Int      @map("file_size")
  mimeType    String?  @map("mime_type")
  entityType  String   @map("entity_type")
  entityId    String   @map("entity_id")
  outputInvoiceId String? @map("output_invoice_id")
  outputInvoice OutputInvoice? @relation(fields: [outputInvoiceId], references: [id])
  inputInvoiceId  String? @map("input_invoice_id")
  inputInvoice  InputInvoice? @relation(fields: [inputInvoiceId], references: [id])
  uploadedBy  String?  @map("uploaded_by")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")

  @@map("invoice_files")
}

model InvoiceDeliveryRecord {
  id              String        @id @default(cuid())
  invoiceId       String        @map("invoice_id")
  invoice         OutputInvoice @relation(fields: [invoiceId], references: [id])
  deliveryMethod  String @map("delivery_method")
  recipientEmail  String?       @map("recipient_email")
  status          String @default("PENDING")
  deliveredAt     DateTime?     @map("delivered_at")
  downloadCount   Int           @default(0) @map("download_count")
  printCount      Int           @default(0) @map("print_count")
  errorMessage    String?       @map("error_message")
  createdAt       DateTime      @default(now()) @map("created_at")
  updatedAt       DateTime      @updatedAt @map("updated_at")

  @@map("invoice_delivery_records")
}

model InvoiceVerifyRecord {
  id              String       @id @default(cuid())
  invoiceId       String       @map("invoice_id")
  invoice         OutputInvoice @relation(fields: [invoiceId], references: [id])
  inputInvoiceId  String?      @map("input_invoice_id")
  inputInvoice    InputInvoice? @relation(fields: [inputInvoiceId], references: [id])
  verifyStatus    String @map("verify_status")
  verifyResult    String?        @map("verify_result")
  verifiedAt      DateTime?    @map("verified_at")
  errorMessage    String?      @map("error_message")
  createdAt       DateTime     @default(now()) @map("created_at")
  updatedAt       DateTime     @updatedAt @map("updated_at")

  @@map("invoice_verify_records")
}

model ArchiveRecord {
  id            String        @id @default(cuid())
  archiveNo     String        @unique @map("archive_no")
  entityType    String        @map("entity_type")
  entityId      String        @map("entity_id")
  archiveStatus String @default("PENDING_ARCHIVE") @map("archive_status")
  archivedBy    String?       @map("archived_by")
  archivedAt    DateTime?     @map("archived_at")
  voucherNo     String?       @map("voucher_no")
  remark        String?
  attachments   String          @default("[]")
  createdAt     DateTime      @default(now()) @map("created_at")
  updatedAt     DateTime      @updatedAt @map("updated_at")

  @@map("archive_records")
}

model OperationLog {
  id          String   @id @default(cuid())
  userId      String?  @map("user_id")
  user        User?    @relation(fields: [userId], references: [id])
  action      String
  entityType  String   @map("entity_type")
  entityId    String?  @map("entity_id")
  oldValue    String?    @map("old_value")
  newValue    String?    @map("new_value")
  ipAddress   String?  @map("ip_address")
  userAgent   String?  @map("user_agent")
  createdAt   DateTime @default(now()) @map("created_at")

  @@map("operation_logs")
}

model RiskResult {
  id          String    @id @default(cuid())
  ruleCode    String    @map("rule_code")
  ruleName    String    @map("rule_name")
  riskLevel   String @map("risk_level")
  entityType  String    @map("entity_type")
  entityId    String    @map("entity_id")
  description String?
  suggestion  String?
  isResolved  Boolean   @default(false) @map("is_resolved")
  resolvedBy  String?   @map("resolved_by")
  resolvedAt  DateTime? @map("resolved_at")
  createdAt   DateTime  @default(now()) @map("created_at")
  updatedAt   DateTime  @updatedAt @map("updated_at")

  @@map("risk_results")
}

model WorkflowDefinition {
  id          String   @id @default(cuid())
  entityType  String   @map("entity_type")
  name        String
  steps       String   @default("[]")
  isActive    Boolean  @default(true) @map("is_active")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  @@map("workflow_definitions")
}

model WorkflowInstance {
  id          String   @id @default(cuid())
  entityType  String   @map("entity_type")
  entityId    String   @map("entity_id")
  entityTitle String?  @map("entity_title")
  totalSteps  Int      @default(1) @map("total_steps")
  currentStep Int      @default(0) @map("current_step")
  status      String   @default("IN_PROGRESS")
  stepsData   String   @default("[]") @map("steps_data")
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @updatedAt @map("updated_at")
  records     ApprovalRecord[]
  @@map("workflow_instances")
}

model ApprovalRecord {
  id          String   @id @default(cuid())
  instanceId  String   @map("instance_id")
  instance    WorkflowInstance @relation(fields: [instanceId], references: [id])
  stepOrder   Int      @map("step_order")
  approverId  String   @map("approver_id")
  approverName String? @map("approver_name")
  action      String
  comment     String?
  createdAt   DateTime @default(now()) @map("created_at")
  @@map("approval_records")
}

model PreInvoice {
  id                String        @id @default(cuid())
  preInvoiceNo      String        @unique @map("pre_invoice_no")
  sourceType        String        @default("APPLICATION") @map("source_type")
  applicationId     String?       @unique @map("application_id")
  application       OutputInvoiceApplication? @relation(fields: [applicationId], references: [id])
  taxSubjectId      String        @map("tax_subject_id")
  taxSubject        TaxSubject    @relation(fields: [taxSubjectId], references: [id])
  customerId        String        @map("customer_id")
  customer          Customer      @relation(fields: [customerId], references: [id])
  buyerName         String        @map("buyer_name")
  buyerTaxNo        String?       @map("buyer_tax_no")
  buyerAddress      String?       @map("buyer_address")
  buyerPhone        String?       @map("buyer_phone")
  buyerBankName     String?       @map("buyer_bank_name")
  buyerBankAccount  String?       @map("buyer_bank_account")
  sellerName        String        @default("") @map("seller_name")
  sellerTaxNo       String?       @map("seller_tax_no")
  invoiceCategory   String        @map("invoice_category")
  amountWithoutTax  Decimal       @default(0) @map("amount_without_tax")
  taxAmount         Decimal       @default(0) @map("tax_amount")
  amountWithTax     Decimal       @default(0) @map("amount_with_tax")
  currency          String        @default("CNY")
  remark            String?
  reviewStatus      String        @default("DRAFT") @map("review_status")
  firstReviewComment  String?     @map("first_review_comment")
  firstReviewedAt     DateTime?   @map("first_reviewed_at")
  secondReviewComment String?     @map("second_review_comment")
  secondReviewedAt    DateTime?   @map("second_reviewed_at")
  taxPushStatus     String        @default("PENDING") @map("tax_push_status")
  taxPushAt         DateTime?     @map("tax_push_at")
  taxResponseData   String?       @map("tax_response_data")
  outputInvoiceId   String?       @unique @map("output_invoice_id")
  outputInvoice     OutputInvoice? @relation(fields: [outputInvoiceId], references: [id])
  deliveryStatus    String        @default("PENDING") @map("delivery_status")
  attachments       String        @default("[]")
  createdAt         DateTime      @default(now()) @map("created_at")
  updatedAt         DateTime      @updatedAt @map("updated_at")

  items PreInvoiceItem[]

  @@map("pre_invoices")
}

model PreInvoiceItem {
  id                    String      @id @default(cuid())
  preInvoiceId          String      @map("pre_invoice_id")
  preInvoice            PreInvoice  @relation(fields: [preInvoiceId], references: [id], onDelete: Cascade)
  appItemId             String?     @map("app_item_id")
  itemName              String      @map("item_name")
  taxClassificationCode String?     @map("tax_classification_code")
  invoiceItemId         String?     @map("invoice_item_id")
  invoiceItem           InvoiceItem? @relation(fields: [invoiceItemId], references: [id])
  taxCodeId             String?     @map("tax_code_id")
  taxCode               TaxCode?    @relation(fields: [taxCodeId], references: [id])
  spec                  String?
  unit                  String?
  quantity              Decimal     @default(1)
  unitPrice             Decimal     @default(0) @map("unit_price")
  amount                Decimal     @default(0)
  taxRate               Decimal     @default(0) @map("tax_rate")
  taxAmount             Decimal     @default(0) @map("tax_amount")
  totalAmount           Decimal     @default(0) @map("total_amount")
  feeType               String?     @map("fee_type")
  sortOrder             Int         @default(0) @map("sort_order")
  createdAt             DateTime    @default(now()) @map("created_at")
  updatedAt             DateTime    @updatedAt @map("updated_at")

  @@map("pre_invoice_items")
}

model TaxSubjectBankAccount {
  id           String     @id @default(cuid())
  taxSubjectId String     @map("tax_subject_id")
  taxSubject   TaxSubject @relation(fields: [taxSubjectId], references: [id])
  bankName     String     @map("bank_name")
  bankAccount  String     @map("bank_account")
  isDefault    Boolean    @default(false) @map("is_default")
  createdAt    DateTime   @default(now()) @map("created_at")

  @@map("tax_subject_bank_accounts")
}

model CostEntry {
  id               String        @id @default(cuid())
  entryNo          String        @unique @map("entry_no")
  businessOrderId  String        @map("business_order_id")
  businessOrder    BusinessOrder @relation(fields: [businessOrderId], references: [id])
  supplierId       String?       @map("supplier_id")
  supplier         Supplier?     @relation(fields: [supplierId], references: [id])
  costCenterId     String?       @map("cost_center_id")
  costCenter       CostCenter?   @relation(fields: [costCenterId], references: [id])
  costType         String?       @map("cost_type")
  description      String?
  amount           Decimal       @default(0)
  currency         String        @default("CNY")
  occurredDate     DateTime?     @map("occurred_date")
  f01Status        String        @default("PENDING") @map("f01_status")
  c01Status        String        @default("PENDING") @map("c01_status")
  status           String        @default("DRAFT")
  inputInvoiceId   String?       @map("input_invoice_id")
  remark           String?
  attachments      String        @default("[]")
  createdBy        String?       @map("created_by")
  createdAt        DateTime      @default(now()) @map("created_at")
  updatedAt        DateTime      @updatedAt @map("updated_at")

  @@map("cost_entries")
}
